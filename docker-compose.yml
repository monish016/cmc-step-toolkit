version: "3.8"

services:
  step-toolkit:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - uploads:/tmp/step_uploads
    restart: unless-stopped

volumes:
  uploads:
